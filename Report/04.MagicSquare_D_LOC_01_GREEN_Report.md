# MagicSquare_1004 — D-LOC-01 GREEN 보고서

## 문서 정보

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_1004 |
| 문서 | Logic Track 첫 GREEN — D-LOC-01 (FR-LOC-01) |
| 선행 | STEP 3 Skill·Command — `Report/03.*` · RED — `red` 브랜치 |
| Phase | **GREEN** \| Layer: **entity** \| Track: **Logic** |
| 상태 | D-LOC-01 **GREEN PASS** · Golden Master 기준선 확정 · REFACTOR 미착수 |

---

## 1. 개요

Harness(STEP 2)와 Agent 절차(STEP 3) 이후, **도메인 코드 첫 GREEN**을 `find_blank_coords`로 완료했다.

| 항목 | 내용 |
|------|------|
| Test ID | **D-LOC-01** |
| FR | **FR-LOC-01** — 4×4 부분 격자에서 빈칸(`0`) 2곳 좌표를 row-major·1-index로 반환 |
| API | `find_blank_coords(grid: list[list[int]]) -> list[tuple[int, int]]` |
| G1 기대 | `[(2, 2), (3, 3)]` |

---

## 2. RED 재확인

`green` 브랜치에 `red`의 RED 산출물을 반영한 뒤 pytest 실행:

```text
FAILED tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major
Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패
```

의도적 `pytest.fail` — GREEN 착수 조건 충족.

---

## 3. 구현 (최소 GREEN)

### 3.1 `src/entity/constants.py`

| 상수 | 값 | 용도 |
|------|-----|------|
| `GRID_SIZE` | 4 | 격자 순회 |
| `BLANK_CELL` | 0 | 빈칸 판별 (신규) |
| `MAGIC_SUM` | 34 | SSOT (향후 validate) |
| `CELL_MAX` | 16 | SSOT |

### 3.2 `src/entity/location.py`

- `GRID_SIZE`×`GRID_SIZE` row-major 스캔
- `grid[row][col] == BLANK_CELL` → `(row + 1, col + 1)` 1-index 튜플 수집
- boundary/control import 없음 · E001~E005 없음

### 3.3 테스트

| 파일 | 변경 |
|------|------|
| `tests/entity/test_d_loc_01.py` | AAA + `assert find_blank_coords(grid_g1) == [(2, 2), (3, 3)]` |
| `tests/conftest.py` | `grid_g1` 픽스처 + `find_blank_coords` 픽스처 (importlib) |

---

## 4. Harness 이슈 — `tests/entity` vs `src/entity`

pytest 수집 시 `tests/entity` 패키지가 `src/entity`를 가려 `ModuleNotFoundError: entity.location` 발생.

**대응:** `tests/conftest.py`에서 `importlib`로 `entity.constants`·`entity.location`을 `sys.modules`에 등록 후 로드.  
테스트는 **픽스처**로 실제 구현 함수를 주입 — Domain Mock 아님.

```
tests/entity/  (pytest 패키지 이름 충돌)
       ↓ importlib 우회
src/entity/location.py  → find_blank_coords
```

---

## 5. GREEN PASS · Golden Master

| 항목 | 내용 |
|------|------|
| GREEN PASS | Skill 5~7단계 충족 — 대상·디렉터리·`python -m pytest` 전부 PASS |
| Golden Master | `tests/golden/d_loc_01_g1_baseline.json` — G1 → `[[2,2],[3,3]]` (1-index) |
| 다음 단계 | Golden Master 검증 턴 또는 `green` 커밋 후 REFACTOR |

---

## 6. pytest 결과

| 명령 | 결과 |
|------|------|
| `tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major` | 1 passed |
| `tests/entity/test_d_loc_01.py` | 1 passed |
| `tests/entity/` | 1 passed |

**환경:** Windows, Python 3.10.11, pytest 9.0.3 (전역 venv; `.venv` 사용 권장 — README·본 세션 Q&A 참고)

### REPL 스모크

```python
# G1 격자 → [(2, 2), (3, 3)]
find_blank_coords([[16,3,2,13],[5,0,11,12],[9,6,0,12],[4,15,14,1]])
```

---

## 7. ECB·Dual-Track 체크리스트

| 규칙 | D-LOC-01 GREEN |
|------|----------------|
| C1 import 방향 (entity → \*) | ✅ |
| C2 entity E001~E005 | ✅ 미사용 |
| C4 MagicConstant SSOT | ✅ `constants.py`만 |
| C5 Logic Domain Mock | ✅ 실제 entity 로드 |
| RED 묶음 외 ID 동시 구현 | ✅ 없음 |
| REFACTOR | ✅ 미수행 |

---

## 8. 프로젝트 구조 (STEP 4 이후)

```
MagicSquare_xx/
├── src/entity/
│   ├── constants.py    ← SSOT
│   └── location.py     ← find_blank_coords (신규)
├── tests/
│   ├── conftest.py     ← grid_g1, find_blank_coords 픽스처
│   └── entity/
│       └── test_d_loc_01.py
├── red/                ← RED 스켈레톤 브랜치 (Git)
└── green/              ← GREEN 작업 브랜치 (현재)
```

---

## 9. Git 상태

| 항목 | 내용 |
|------|------|
| 브랜치 | `green` (작업), `red` (RED 스켈레톤) |
| 최신 커밋 (green) | `997c3bf` — STEP 3 |
| STEP 4 변경 | **로컬 unstaged** — commit은 사용자 요청 시 |

---

## 10. 다음 RED/GREEN 후보

| 우선순위 | ID | 비고 |
|----------|-----|------|
| 1 | D-LOC-02+ | LOC 묶음 설계표 (RED 플랜 세션) |
| 2 | D-001 | `reference.md` — `sum_line` |
| — | PRD | FR-LOC-01 formal 등재 (v0.2) |

---

## 11. 참고

| 문서 | 설명 |
|------|------|
| `Report/04.REPORT.md` | STEP 4 세션 요약 |
| `Prompting/04.cursor_d_loc_01_green_discussion.md` | Cursor 대화 Export |
| `.cursor/skills/magic-square-tdd/SKILL.md` | GREEN 절차 |
| `.cursorrules` | 도메인·ECB SSOT |
